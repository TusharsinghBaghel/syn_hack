spring.application.name=demo

server.port=8080

# SAFE: Replaced hard-coded password with an environment variable
spring.data.mongodb.uri=mongodb+srv://sohamanwane111_db_user:${MONGO_DB_PASSWORD}@synhackcluster.uv3ig6o.mongodb.net/system_simulator?retryWrites=true&w=majority
spring.data.mongodb.database=system_simulator

spring.data.mongodb.auto-index-creation=true

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=bt22ece019@students.vnit.ac.in
# SAFE: Replaced hard-coded password with an environment variable
spring.mail.password=${EMAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# SAFE: Replaced hard-coded secret with an environment variable
app.jwt.secret=${JWT_SECRET}
app.jwt.expiration=86400000 

# SAFE: Replaced hard-coded secrets with environment variables
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=profile,email
spring.security.oauth2.client.registration.google.redirect-uri={baseUrl}/login/oauth2/code/google

spring.main.allow-bean-definition-overriding=true
logging.level.org.springframework.data.mongodb.core.MongoTemplate=INFO